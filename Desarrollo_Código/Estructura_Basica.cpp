#include <map>
#include <string>
using namespace std;
class Environment {
private:
    map<std::string, Variant> symbolTable; // Tabla de símbolos

public:
    // Métodos para manipular la tabla de símbolos
};